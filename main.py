from comunidade import app

  

if __name__ == '__main__':
    #quando for colocar no ar tem que tirar o debug=true
    #o debug = true serve para não precisar para a aplicação e rodar novamente para atulizar o site
    app.run(debug=True)